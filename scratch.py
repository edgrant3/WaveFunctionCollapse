from enum import Enum
import numpy as np
import heapq as hq

class Scratch(Enum):
    BUTT = 1
    ARSE = 2
    TUSH = 3


if __name__ == "__main__":

    # heap = []
    # # hq.heappush(heap, (2, (2,3)))
    # # hq.heappush(heap, (3, (1,2)))
    # # hq.heappush(heap, (1, (3,4)))

    # hq.heappush(heap, 2)
    # hq.heappush(heap, 3)
    # hq.heappush(heap, 1)
    # hq.heappush(heap, 5)
    
    # print(heap)

    # while len(heap):
    #     hq.heappop(heap)
    #     print(heap)

    # hq = [False]
    # if(hq):
    #     print(hq)

    # a = (1, (2,3))
    # print(a[1])

    a = True
    b = False
    print(a and b)